[project]
name = "syncwalk-api"
version = "0.1.0"
description = "SyncWalk audio tour platform API"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
